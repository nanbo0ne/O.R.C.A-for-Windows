# Wails build assets/output — mostly auto-generated (plists, manifests, compiled
# binary); ignore them but keep the committed designed app icon.
/build/*
!/build/appicon.png

# Commit only our customized per-user NSIS installer template. wails_tools.nsh and
# everything else under build/ are regenerated by `wails build`, so stay ignored.
# (Each `!` un-ignore needs its parent dir un-ignored first.)
!/build/windows
/build/windows/*
!/build/windows/info.json
!/build/windows/icon.ico
!/build/windows/installer
/build/windows/installer/*
!/build/windows/installer/project.nsi
!/build/windows/installer/installer_guard.nsh
!/build/windows/installer/resources
!/build/windows/installer/resources/*

# Keep our XML-escaped Wails templates and hardened-runtime entitlements.
!/build/darwin
/build/darwin/*
!/build/darwin/entitlements.plist
!/build/darwin/Info.plist
!/build/darwin/Info.dev.plist

# Commit the hand-written Linux .deb packaging inputs (nfpm config + .desktop entry).
# `wails build` does not generate these, so they must live in git. The .deb is built
# by scripts/desktop-build.sh's linux branch via goreleaser/nfpm. Parent un-ignored first.
!/build/linux
/build/linux/*
!/build/linux/nfpm.yaml
!/build/linux/orca.desktop

# Go test binaries
*.test

# Local config symlinks for `wails dev` (point at the repo-root deepseek-orca.toml/.env
# so the desktop app resolves the same config/keys the CLI does). Never commit.
/deepseek-orca.toml
/.env

# `go build` binary produced in-tree (module deepseek-orca/desktop → "desktop")
/desktop
# Wails dependency-change cache
/frontend/package.json.md5
